"""Report-only dependency-audit tooling (not CI gates): what the tree imports, how current every
action/hook pin is against upstream, and an offline OSV cross-check for PyPI and Ubuntu packages.
The gates live one directory up; these scripts reproduce the dated audits in docs/security/."""
