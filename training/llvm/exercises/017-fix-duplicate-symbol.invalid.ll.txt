; Intentionally invalid: the module defines @adjust twice.
source_filename = "017-fix-duplicate-symbol.invalid.ll.txt"

define i32 @adjust(i32 %x) {
entry:
  %y = add i32 %x, 1
  ret i32 %y
}

define i32 @adjust(i32 %x) {
entry:
  %y = sub i32 %x, 1
  ret i32 %y
}
