{
  "schema": "bcir-irdl-manifest.v1",
  "naming_rule": "dots-to-underscores",
  "note": "The IRDL projection (bcir.irdl.mlir) covers the structural subset of the dialect. IRDL admits only [a-z0-9_] operation names, so a dotted ODS name is spelled with underscores (gem.stream_pack -> gem_stream_pack): that is the projection's one naming deviation, applied as a rule by tools/irdl/check_inventory.py, never tabulated per operation. Every ODS operation is either projected under that rule or listed here with the reason it is not; the gate refuses an undeclared gap, a stale entry, a ghost, an orphan projection and a naming collision, and the quick tier runs it on every host (bcir/tests/test_irdl_inventory.py).",
  "unprojected": {
    "abi_contract": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "asm": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "atomic_cas": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "atomic_rmw": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "creg_read": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "creg_write": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "descriptor_load": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "device_manifest": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "ecn.class": "ECN operation (X.692, R24/R25) that landed after the projection; not yet projected",
    "ecn.condition": "ECN operation (X.692, R24/R25) that landed after the projection; not yet projected",
    "ecn.field": "ECN operation (X.692, R24/R25) that landed after the projection; not yet projected",
    "ecn.module": "ECN operation (X.692, R24/R25) that landed after the projection; not yet projected",
    "ecn.object": "ECN operation (X.692, R24/R25) that landed after the projection; not yet projected",
    "ecn.parameterized": "ECN operation (X.692, R24/R25) that landed after the projection; not yet projected",
    "ecn.structure": "ECN operation (X.692, R24/R25) that landed after the projection; not yet projected",
    "entry": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "gem.activation": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.attention": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.autodiff": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.contention": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.conv": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.embedding": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.fused_matmul_activation": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.gqa_attention": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.kv_cache": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.layout_pivot": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.matmul": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.matmul_buffer": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.rmsnorm": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "gem.rope": "GEM model operation (matmul/attention/norm/cache/embedding seams and their lowering buffers) that landed after the projection; not yet projected",
    "interrupt_trampoline": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "msr_read": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "msr_write": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "portio": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "segment_reload": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "task_register_load": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "volatile_load": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it",
    "volatile_store": "driver-subset core operation (values, registers, ports, descriptors, entry/trampoline): outside the structural subset -- registry, claim, phase, plan, GEM stream -- the projection covers today; project it together with an IRDL corpus fixture that exercises it"
  }
}
